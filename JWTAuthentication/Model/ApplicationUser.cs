using Microsoft.AspNetCore.Identity;

namespace JWTAuthentication.Models
{
    public class ApplicationUser : IdentityUser
    {
        // Add custom properties here if needed (e.g., FirstName, Department, etc.)
    }
}
